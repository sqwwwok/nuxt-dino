export default function ( {app,redirect,store,route} ) {
  // if(!store.state.userData.username){
  //   app.$axios.get('/login')
  //   .then(res => {
  //     if(route.path==='/login-register') redirect('/');
  //     store.commit({
  //       type: 'recordUser',
  //       username: res.data.username,
  //       historys: res.data.historys,
  //     })
  //   }).catch(e => {
  //     if(route.path!=='/login-register') redirect('/login-register')
  //   })
  // }
}